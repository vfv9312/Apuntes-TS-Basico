//@ts-ckech
//npx tsc ./src/Atrapar-bug/demon.ts --target es6   ---> esto convierte a js en un emacscript que elijamos
//npx tsc src/*.ts --target es2022 --outDir dist.  ---> podemos idicar que se guarde el archivo js en alguna otra carpeta * significa todos los tsc pero podemos elejir solor una en particular

(async()=> {
  const myCart = [];
  const products = [];
  const limit = 2;

  async function getProducts() {
    const rta = await fetch('http://api.escuelajs.co/api/v1/products', {
      method: 'GET'
    });
    const data = await rta.json();
    products.concat(data);
  }
  function getTotal() {
    let total = 0;
    for (let i = 0; i < products.length; i++) {
      total += products[i];
    }
    return total;
  }
  function addProduct(index) {
    if (getTotal() <= limit) {
      myCart.push(products[index]);
    }
  }

  await getProducts();
  addProduct(1);
  addProduct(2);
  const total = getTotal();
  console.log(total);
  const person = {
    name: 'Nicolas',
    lastName: 'Molina'
  }
  // const rta = person +  limit;
  // console.log(rta);
});
